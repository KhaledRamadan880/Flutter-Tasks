import 'package:flutter/material.dart';

class AppIcons {
  static const Icon arrowRight = Icon(Icons.arrow_forward);
  static const Icon arrowLeft = Icon(Icons.arrow_back);
  static const Icon arrowDown = Icon(Icons.arrow_downward);
  static const Icon name = Icon(Icons.person);
  static const Icon phone = Icon(Icons.phone);
  static const Icon mail = Icon(Icons.mail_outline);
  static const Icon lock = Icon(Icons.lock);
  static const Icon eyeOff = Icon(Icons.visibility_off);
  static const Icon eyeon = Icon(Icons.visibility);
}
